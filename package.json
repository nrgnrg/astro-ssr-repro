{
  "name": "@example/minimal",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "pnpm build && wrangler pages dev ./dist"
  },
  "devDependencies": {
    "astro": "^1.0.0-beta.52"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^0.2.0",
    "web-streams-polyfill": "^3.2.1",
    "wrangler": "^2.0.15"
  }
}